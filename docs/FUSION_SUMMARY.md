# OpenClaw 融合项目总结

> **项目完成时间**: 2026-04-27
> **项目分支**: feature/fusion-claude-hermes
> **核心理念**: 强化 OpenClaw，使其具备强大的自主持久工作能力和自主技能形成能力

---

## 一、项目背景与目标

### 1.1 融合动机

OpenClaw 是一个功能完整的 AI Agent 平台，但在实际使用中发现两个关键能力缺口：

| 缺口                 | 表现                                                       | 影响                 |
| -------------------- | ---------------------------------------------------------- | -------------------- |
| **自主持久工作能力** | Agent 无法长时间稳定完成复杂编程任务，容易中断或陷入死循环 | 复杂任务完成率低     |
| **自主技能形成能力** | Agent 无法从有效工作流程中"学习"并固化可复用技能           | 重复探索，效率不提升 |

### 1.2 融合来源

我们研究了两个优秀项目来解决这些问题：

**Claude Code** (Anthropic 官方 CLI):

- **12层 Harness 机制**: 让 Agent 能长时间稳定完成复杂编程任务
- 核心创新：StreamingToolExecutor、Fork 缓存优化、Coordinator 空闲循环、Background Tasks

**Hermes Agent** (开源 Agent):

- **skill_manage 工具**: Agent 从有效流程中学习并固化可复用技能
- 核心创新：学习触发检测、工作流程提取、规则持久化

### 1.3 项目目标

```
目标: 强化 OpenClaw
├── 自主持久工作能力 (来自 Claude Code)
│   ├── 权限安全决策 (Phase 6)
│   ├── 上下文管理 (Phase 2)
│   ├── 记忆系统 (Phase 1)
│   ├── 终端安全执行 (Phase 3)
│   ├── 流式并发执行 (Phase 10)
│   ├── Fork 缓存优化 (Phase 11)
│   ├── Coordinator 空闲循环 (Phase 12)
│   └── Background Tasks (Phase 13)
│
└── 自主技能形成能力 (来自 Hermes Agent)
    ├── skill_manage 工具 (Phase 8)
    └── 规则持久化 (Phase 9)
```

---

## 二、实施计划与成果

### 2.1 Phase 清单

| Phase              | 能力来源             | 实现内容                    | 文件数 | 测试数 | 状态      |
| ------------------ | -------------------- | --------------------------- | ------ | ------ | --------- |
| **Phase 1**        | Claude Code          | 记忆系统 (memory 模块)      | 8      | 43     | ✅        |
| **Phase 2**        | Claude Code          | 熔断器 + 递归保护           | 7      | 35     | ✅        |
| **Phase 3**        | Claude Code + Hermes | 危险命令检测 + sandbox 桥接 | 11     | 41     | ✅        |
| **Phase 4**        | 基础增强             | 技能查询工具                | 2      | 12     | ✅        |
| **Phase 5**        | 基础增强             | 任务系统                    | 4      | 23     | ✅        |
| **Phase 6**        | Claude Code + Hermes | 权限系统 + 审批流程         | 10     | 45+    | ✅        |
| **Phase 7**        | OpenClaw 原生        | 极致缓存利用                | -      | -      | ✅ 已覆盖 |
| **Phase 8**        | Hermes Agent         | 自主技能形成 (skill_manage) | 2      | 20     | ✅        |
| **Phase 9**        | Hermes Agent         | 规则持久化                  | 2      | 23     | ✅        |
| **Phase 10**       | Claude Code          | StreamingToolExecutor       | 1      | 18     | ✅        |
| **Phase 11**       | Claude Code          | Fork 缓存优化               | 1      | 18     | ✅        |
| **Phase 12**       | Claude Code          | Coordinator 空闲循环        | 1      | 25     | ✅        |
| **Phase 13**       | Claude Code          | Background Tasks            | 1      | 24     | ✅        |
| **Phase 14**       | Claude Code          | 运行时集成                  | 2      | 15     | ✅        |
| **Phase 14-adapt** | 架构适配             | 外围层适配实现              | 4      | 78     | ✅        |

### 2.2 总测试覆盖

```
测试统计:
├── 核心模块: 43 + 35 + 41 + 45 + 20 + 23 = 167 tests
├── 持久工作能力: 18 + 18 + 25 + 24 + 15 = 80 tests
├── 适配版实现: 78 tests
├── 权限审批 e2e: 28 tests
└── 全量测试: 1495 tests ✅
```

### 2.3 关键实现文件

| 模块        | 文件路径                                        | 功能描述                              |
| ----------- | ----------------------------------------------- | ------------------------------------- |
| 记忆系统    | `src/memory/`                                   | Agent 记忆索引、分类、截断            |
| 熔断器      | `src/agents/compaction/`                        | 连续失败熔断、递归保护                |
| 危险检测    | `src/agents/terminal/`                          | 30+ 危险命令模式检测                  |
| 权限系统    | `src/agents/permissions/`                       | PermissionMode、规则匹配、审批流程    |
| 技能管理    | `src/agents/skills/skill-manage-tool.ts`        | create/edit/patch/delete 技能         |
| 规则持久化  | `src/agents/permissions/persistence.ts`         | 用户审批决策持久化                    |
| 并发控制    | `src/agents/concurrency-control-wrapper.ts`     | 工具执行槽位管理                      |
| 批量 Spawn  | `src/agents/tools/sessions-spawn-batch-tool.ts` | 2-10 子 Agent 批量启动 + Prompt Cache |
| Coordinator | `src/agents/coordinator-loop-manager.ts`        | 空闲循环 + 自动任务认领               |
| Background  | `src/agents/tools/background-task-tool.ts`      | 后台持久执行                          |

---

## 三、与原版 OpenClaw 的区别

### 3.1 新增能力

| 能力                 | 原版 OpenClaw   | 融合版 OpenClaw               | 增益             |
| -------------------- | --------------- | ----------------------------- | ---------------- |
| **记忆系统**         | 无结构化记忆    | 类型化记忆 + 自动索引         | Agent 可积累知识 |
| **熔断器**           | 无失败保护      | 连续失败自动熔断              | 防止死循环       |
| **危险命令检测**     | 无安全检测      | 30+ 模式检测 + 审批流程       | 安全执行         |
| **权限审批**         | 简单 allow/deny | PermissionMode + Gateway 审批 | 用户可控安全     |
| **技能学习**         | 手动创建技能    | Agent 自主创建/编辑技能       | 持续进化         |
| **批量 Spawn**       | 单个 spawn      | 批量 spawn + Prompt Cache     | 效率提升         |
| **Coordinator**      | 无空闲循环      | 空闲自动认领任务              | 持久工作         |
| **Background Tasks** | 无后台执行      | 后台持久执行                  | 长时间任务       |

### 3.2 架构差异处理

Claude Code 的某些核心设计不适用于 OpenClaw 的架构，我们采用外围适配策略：

| 原始设计              | 原始位置            | OpenClaw 适配位置              | 原因                              |
| --------------------- | ------------------- | ------------------------------ | --------------------------------- |
| StreamingToolExecutor | 替换 Tool.ts 执行器 | concurrency-control-wrapper.ts | SessionManager 库控制执行         |
| Fork Cache            | 批量 Fork 消息构建  | sessions-spawn-batch-tool.ts   | OpenClaw 使用 session-based spawn |
| Coordinator 空闲循环  | run.ts 主循环       | coordinator-loop-manager.ts    | OpenClaw 运行完成即返回           |

**策略**: 不修改 OpenClaw 核心代码（run.ts、SessionManager），通过外围层实现相同能力。

### 3.3 sandbox 模块职责分离

原计划将 terminal 模块与 sandbox 模块统一，最终采用方案 C（职责分离）：

| 模块               | 职责                                          | 代码位置                   |
| ------------------ | --------------------------------------------- | -------------------------- |
| terminal           | 命令级安全检测 (30+ 模式) + local 执行        | `src/agents/terminal/`     |
| sandbox            | 容器级隔离 (Docker/SSH) + workspace lifecycle | `src/agents/sandbox/`      |
| bash-tools.exec.ts | 桥接协调：危险检测 → sandbox 执行             | Line 1511-1526 → Line 1688 |

**验证**: 危险检测在 sandbox 执行之前生效 ✅

---

## 四、核心优势

### 4.1 安全执行能力

```
执行安全链:
┌─────────────────────────────────────────────────────────────┐
│  用户请求                                                    │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Phase 6: 权限系统                                           │
│  ├── PermissionMode: default/plan/bypassPermissions/dontAsk │
│  ├── 规则匹配: allow/ask/deny                                │
│  └── Gateway 审批: 用户确认                                  │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Phase 3: 危险命令检测                                        │
│  ├── 30+ 模式: rm -rf, chmod 777, fork bomb, curl | sh...   │
│  ├── Obfuscation 防护: ANSI/null byte/Unicode fullwidth     │
│  └── Session approval 缓存                                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Sandbox 模块 (可选)                                         │
│  ├── Docker 容器隔离                                         │
│  ├── 安全加固: capDrop, no-new-privileges, pidsLimit        │
│  └── Workspace 挂载                                          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  命令执行                                                    │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 持久工作能力

```
持久工作流程:
┌─────────────────────────────────────────────────────────────┐
│  Phase 12: Coordinator 空闲循环                              │
│  ├── 检测空闲状态                                            │
│  ├── 自动认领待执行任务                                      │
│  └── AbortSignal 支持中断                                    │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Phase 13: Background Tasks                                  │
│  ├── 后台持久执行                                            │
│  ├── 模式: run/session/coordinator                           │
│  └── 状态追踪                                                │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Phase 2: 熔断器 + 递归保护                                   │
│  ├── 连续失败 ≥ 5 次 → 自动暂停                              │
│  ├── session_memory Agent 不触发压缩                         │
│  └── 防止无限递归                                            │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 学习进化能力

```
技能形成流程:
┌─────────────────────────────────────────────────────────────┐
│  任务执行                                                    │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  学习触发检测                                                │
│  ├── 复杂任务成功 (5+ 工具调用)                              │
│  ├── 错误后找到有效路径                                      │
│  ├── 用户纠正后成功                                          │
│  └── 发现可复用工作流程                                      │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Phase 8: skill_manage 工具                                  │
│  ├── create: 创建新技能                                      │
│  ├── patch: 针对性修复 (token 高效)                          │
│  ├── edit: 结构性重写                                        │
│  └── delete: 删除过时技能                                    │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Phase 9: 规则持久化                                          │
│  ├── 用户审批决策保存                                        │
│  ├── ~/.openclaw/permissions.json                            │
│  └── Agent 记住用户偏好                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、待优化方向

### 5.1 已识别的优化点

| 方向                               | 当前状态                          | 优化建议                 | 优先级 |
| ---------------------------------- | --------------------------------- | ------------------------ | ------ |
| **Prompt Cache 深度优化**          | sessions-spawn-batch 实现基础缓存 | 进一步优化消息前缀稳定性 | 中     |
| **Coordinator 自动触发**           | 需手动启动 coordinator 模式       | 集成到 Agent 生命周期    | 中     |
| **技能学习触发自动化**             | 需 Agent 主动调用 skill_manage    | 基于行为模式自动触发     | 低     |
| **权限配置 UI**                    | 仅 CLI 配置                       | 添加 Control UI 配置界面 | 低     |
| **modal/daytona/singularity 后端** | 未实现                            | 扩展 sandbox 后端支持    | 低     |

### 5.2 架构演进建议

```
短期优化 (1-2 周):
├── Coordinator 自动启动: 在 run.ts 检测空闲时自动启动 coordinator
├── 权限配置持久化增强: 扩展 persistence.ts 支持 profile 级持久化
└── Prompt Cache 稳定性测试: 添加回归测试确保前缀稳定

中期优化 (1-2 月):
├── 技能学习自动化: 添加 before_agent_end hook 检测学习机会
├── Control UI 权限管理: 添加权限配置界面
└── Coordinator 多任务调度: 支持优先级队列和依赖管理

长期优化 (3+ 月):
├── modal/daytona/singularity 后端扩展
├── 多 Agent 协作框架: 基于 sessions-spawn-batch 实现团队协作
└── Agent 行为分析: 自动识别和推荐技能创建
```

### 5.3 用户环境配置优化

Doctor 检测发现的环境配置问题：

| 问题                  | 解决方法                         |
| --------------------- | -------------------------------- |
| State integrity       | `openclaw doctor --fix` 自动修复 |
| Memory search API key | 设置 `OPENAI_API_KEY` 等环境变量 |
| Skills symlink 配置   | 检查 symlink 路径配置            |

---

## 六、结论

### 6.1 项目成果

**融合项目圆满完成**：

- ✅ 所有 14 个 Phase 实现完成
- ✅ 1495 测试全部通过
- ✅ 代码已推送到 `feature/fusion-claude-hermes`
- ✅ OpenClaw 启动条件确认

### 6.2 能力对比

| 能力维度     | 原版 OpenClaw | 融合版 OpenClaw                              |
| ------------ | ------------- | -------------------------------------------- |
| **安全执行** | 基础          | ⭐⭐⭐⭐⭐ (权限 + 危险检测 + sandbox)       |
| **持久工作** | 无            | ⭐⭐⭐⭐ (Coordinator + Background + 熔断器) |
| **学习能力** | 无            | ⭐⭐⭐ (skill_manage + 持久化)               |
| **并发效率** | 基础          | ⭐⭐⭐⭐ (批量 spawn + Prompt Cache)         |

### 6.3 最终状态

```
OpenClaw 融合版能力矩阵:

┌─────────────────────────────────────────────────────────────┐
│                    自主持久工作能力                           │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │ 记忆系统 │ │ 熔断器  │ │ 权限系统 │ │ 危险检测 │           │
│  │  ✅     │ │  ✅     │ │  ✅     │ │  ✅     │           │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │ 流式并发 │ │ Fork优化│ │Coordinator│ │Background│           │
│  │  ✅     │ │  ✅     │ │  ✅     │ │  ✅     │           │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    自主技能形成能力                           │
│  ┌─────────────────┐ ┌─────────────────┐                   │
│  │   skill_manage   │ │   规则持久化     │                   │
│  │      ✅          │ │      ✅          │                   │
│  └─────────────────┘ └─────────────────┘                   │
└─────────────────────────────────────────────────────────────┘

OpenClaw 现已具备 Claude Code 的自主持久工作能力 + Hermes Agent 的学习进化能力。
```

---

## 附录：参考文档

- **融合进度跟踪**: `FUSION_PROGRESS.md`
- **Phase 14 适配设计**: `docs/superpowers/specs/2026-04-21-phase14-adaptation-design.md`
- **Phase 14 实现计划**: `docs/superpowers/plans/2026-04-21-phase14-adaptation-impl.md`
- **Phase 3 Terminal 模块**: `src/agents/terminal/IMPLEMENTATION_PROGRESS.md`
